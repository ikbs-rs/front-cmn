import React from 'react';
import { useParams } from 'react-router-dom';
import { useLocation } from 'react-router-dom';
import ObjatttpL from './cmnObjatttpL';

const CmnObjatttpWEnd = (params) => {
    return <ObjatttpL />;
};

export default CmnObjatttpWEnd;
