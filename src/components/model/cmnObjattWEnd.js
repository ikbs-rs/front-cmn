import React from 'react';
import { useParams } from 'react-router-dom';
import { useLocation } from 'react-router-dom';
import ObjattL from './cmnObjattL';

const CmnObjattWEnd = (params) => {
    return <ObjattL />;
};

export default CmnObjattWEnd;
