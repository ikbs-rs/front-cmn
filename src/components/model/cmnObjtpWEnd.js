import React from 'react';
import { useParams } from 'react-router-dom';
import { useLocation } from 'react-router-dom';
import ObjtpL from './cmnObjtpL';

const CmnObjtpWEnd = (params) => {
    return <ObjtpL />;
};

export default CmnObjtpWEnd;
